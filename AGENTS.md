# Workspace Conventions

- Use `just` as the workspace command runner.
- Do not add scripts to the workspace root `package.json`.
- Package-level `package.json` scripts in subfolders are fine.
- Files under `scripts/` should be written in TypeScript.
